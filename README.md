# Serverless backend for ecoify

To sum up the saved carbon, a serverless backend can be deployed.

## Installation

1. `npm install` to install the used npm packages
2. `serverless deploy` to deploy the function
